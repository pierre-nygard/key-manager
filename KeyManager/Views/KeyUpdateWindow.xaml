<Window x:Class="KeyManager.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:KeyManager.Views"
        mc:Ignorable="d"
        Title="Uppdatera" Height="130" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <TextBox Margin="4" Grid.Row="0" Name="NameTextBox" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource MaterialDesignFlatLightBgButton}" ToolTip="Uppdatera" Name="UpdateButton" Click="UpdateButton_Click" FontWeight="Bold" Padding="4" Margin="4 0" Content="Uppdatera" />
            <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatLightBgButton}" Background="#FFff6c84" ToolTip="Avbryt" Name="CancelBtn" Click="CancelBtn_Click"  FontWeight="Bold" Padding="4" Margin="4 0" Content="Avbryt" />
        </Grid>
    </Grid>
</Window>
